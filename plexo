#!/usr/bin/env bash
set -euo pipefail

APP_DIR="$(cd "$(dirname "$0")" && pwd)"
VENV="$APP_DIR/tui/venv"

if [ ! -d "$VENV" ]; then
    echo "→ Setting up virtualenv ..."
    python3 -m venv "$VENV"
    "$VENV/bin/pip" install -q textual 2>/dev/null
fi

exec "$VENV/bin/python" -m tui -- "$@"
